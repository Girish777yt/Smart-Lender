# Smart Lender

A small Flask app that predicts loan approval using a trained ML model.

Setup

```bash
python -m pip install -r requirements.txt
python train_model.py   # trains and saves models/loan_model.pkl
python app.py           # starts the Flask app
```

Open http://127.0.0.1:5000/ and use the mobile-friendly form.
